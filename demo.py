from blinkt import set_pixel, set_brightness, show, clear

set_brightness(0.1)

import time

while True:
    for i in range(8):
        clear()
        set_pixel(i, 255, 255, 0255
        show()
        time.sleep(0.05)